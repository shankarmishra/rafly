{
  "_comment": "Per-route, per-viewport byte budgets, UNCOMPRESSED. These match what inc/tools/audit.mjs measures, which is the dev server with no gzip or brotli. They are NOT what a visitor downloads: production serves mod_deflate + mod_brotli (.htaccess), and the text types compress roughly 5-6x. The phone homepage measures 469 KB here and lands near 240 KB over the wire.",

  "_methodology": "Re-baselined twice. First from scratch for the Machined Paper rebuild, because the budget before it allowed 1250 KB of JS on a homepage whose own note claimed it never loaded three.js (it did, via four separate focal devices) and 170 KB of images for 24 stock photographs since deleted — inheriting it would have meant the redesign passing a test of the thing it replaced. Then again when the 3-D layer was deleted outright, which is the numbers below.",

  "_howToRead": "budgetKB is a ceiling per resource type. Headroom is about 20% over the measured baseline: enough that a new icon or a paragraph does not fail the build, tight enough that a new dependency, an unoptimised image or a script that stopped being gated does.",

  "_theOneThatMatters": "phone / — total 600 and js 80. Every other line is housekeeping; these two are the promise. Nothing on this site loads a 3-D library any more, so the failure mode that budget was written to catch is gone — but the two remaining canvas modules (js/field.js, js/aurora.js) are gated the same way and would fail the same line if a gate broke. It breaks silently, which is why it is measured.",

  "_whyCssRose": "css 190 -> 200. The redesign added css/09-scenes.css (the hero, the head field, the product deck, the phones) and this is the second-largest sheet on the site. Before raising the line, inc/tools/deadcss.mjs was written and run: it found ~7 KB of rules for the deleted 3-D stage still shipping on every page view, and those were removed rather than budgeted for. What is left is a real increase for real new sections, on a figure that gzips to roughly 30 KB over the wire.",

  "_whyTheDefaultCssFELL": "default css 200 -> 175, and this is the more interesting half of the same change. css/09-scenes.css is a CORE sheet, so all sixteen routes downloaded it -- including the hero and its floor, the glass cluster, the product deck, the service bento, the phones and the coverflow gallery, none of which exist outside index.php. Splitting those into css/pages/home-scenes.css took every inner page from 215 KB to 164 KB. The line is set just above the new measurement rather than left at the old one, because a budget that still allows the mistake it just caught is not a budget.",

  "_whyTheLAPTOPCost": "home html 115 -> 125, css 230 -> 245. A whole section was added: a laptop hinged at the bottom edge of its lid that opens across the scroll, with the client portal on its screen. It is the one section on this page whose verb is not \"arrange\" -- the deck fans, the gallery slides, the phones stand still, this one opens -- and its screen is a full dashboard rather than a placeholder, which is where the markup goes. js/macbook.js is gated on min-width 961px, matching the media query that pins the section, so the phone JS figure did not move: below that width the stylesheet declares the laptop open at full size and there is nothing for a module to do.",

  "_whyTheMOCKSCost": "home css 220 -> 230. The deck rendered one shared body -- three KPI blocks and a bar chart -- for all five surfaces, and the three phones rendered one shared screen. Five identical screens tinted five ways read as one product screenshotted five times, so each surface now has its own layout (a code and deploy view, a scan, a channel report, an editor, an order list) and the phones have three different app screens. That is five layouts where there was one and three where there was one, on markup that is otherwise unchanged in size -- the html figure did not move. The orphaned .mock-top .c rule the old shared body left behind was deleted before the line moved.",

  "_whyHomeCssRoseAgain": "home css 200 -> 220. The homepage gained three sections in this pass -- the capability strip, the service bento with its gradient-border cards, and the coverflow gallery built to OriginKit's own geometry -- plus a perspective floor under the hero. It is the only route that pays for them now. Two things were deleted before the line moved rather than after: the whole sticky scrollytelling system (js/scroll.js plus 3.3 KB of CSS for markup no template has ever rendered) and the dead .assembly-* block the bento replaced.",

  "_whyHtmlRose": "home html 100 -> 110. The homepage gained two sections whose markup IS the content — five browser mocks and three phone shells are rendered as elements, not built by JavaScript, which is the whole reason both survive with scripts off. Paying for that in HTML rather than in JS is the correct trade, and it is why the phone js figure fell from 59 KB to 46 KB in the same change.",

  "viewports": {
    "desktop": { "width": 1440, "height": 900 },
    "phone": { "width": 390, "height": 844 }
  },

  "routes": {
    "home": {
      "match": ["/"],
      "note": "No three.js: vendor/three/ is deleted and no page fetches a 3-D library. Desktop's js figure is js/field.js + js/aurora.js + js/decks.js + js/cluster.js + js/interactions.js, every one behind a gate in js/gates.js and none of them fetched before window load plus an idle callback. The phone gets decks.js and nothing else from that list.",
      "desktop": { "html": 125, "css": 245, "js": 300, "font": 90, "img": 120, "total": 850 },
      "phone":   { "html": 125, "css": 245, "js":  80, "font": 90, "img":  60, "total": 620 }
    },
    "blog": {
      "match": ["/blog"],
      "note": "Heaviest img figure of the default set: the listing page's article cover images.",
      "desktop": { "html": 80, "css": 175, "js": 80, "font": 90, "img": 250, "total": 660 },
      "phone":   { "html": 80, "css": 175, "js": 80, "font": 90, "img": 170, "total": 570 }
    },
    "default": {
      "match": ["/about", "/web-development", "/contact", "/web-security", "/marketing-advertisement", "/content-creation", "/ecommerce-support", "/team", "/pricing", "/case-studies", "/privacy", "/locations/greater-noida"],
      "note": "Zero JavaScript beyond the shared tail. partials/head-object.php gives all twelve of these an animated hero ground, and it is pure CSS precisely so this line does not move: a canvas here would have cost a module on twelve pages to decorate a heading, which is the exact mistake the previous build made with three.js.",
      "desktop": { "html": 95, "css": 175, "js": 80, "font": 90, "img": 60, "total": 450 },
      "phone":   { "html": 95, "css": 175, "js": 80, "font": 90, "img": 60, "total": 450 }
    }
  }
}
